#version 450

#include "common.glsl"


//layout(location = LayoutLocation_Position) in vec3 inPos;


// the pipeline automatically handles the depth check
// and updates the depth buffer (after this), so we don't need to
// do anything here.
void main()
{
    // don't do nothing.
}
